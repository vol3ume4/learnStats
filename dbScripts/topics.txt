-- Drop table only if needed
-- DROP TABLE IF EXISTS topics CASCADE;

CREATE TABLE IF NOT EXISTS topics (
    id SERIAL PRIMARY KEY,
    name TEXT NOT NULL UNIQUE,
    teacher_preferred_approach TEXT  -- nullable, teacher can fill later
);

-- Seed topics table
INSERT INTO topics (name, preferred_approach)
VALUES
('Binomial', NULL),
('Normal', NULL),
('Confidence Intervals', NULL),
('CLT', NULL),
('Hypothesis Testing', NULL)
ON CONFLICT (name) DO NOTHING;

-- Verify
SELECT * FROM topics;
